import React from 'react';
import { Helmet } from 'react-helmet';
import Homepage from '../Homepage/Homepage';

const Packages = () => {
  return (
    <div>
      <Homepage page = "/packages" title = "Packages" />
    </div>
  )
};

export default Packages;