import React from 'react';
import { Helmet } from 'react-helmet';
import Homepage from '../Homepage/Homepage';

const FlightsHotels = () => {
  return (
    <div>
      <Homepage page = "/flightshotels" title = "Flights + Hotels" />
    </div>
  )
};

export default FlightsHotels;