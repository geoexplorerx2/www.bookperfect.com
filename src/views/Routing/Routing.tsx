import React from 'react';
import { Helmet } from 'react-helmet';
import Homepage from '../Homepage/Homepage';

const Routing = () => {
  return (
    <div>
      <Homepage page = "/routing" title = "Routing" />
    </div>
  )
};

export default Routing;