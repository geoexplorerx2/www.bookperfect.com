import React from 'react';
import { Helmet } from 'react-helmet';
import Homepage from '../Homepage/Homepage';

const Activities = () => {
  return (
    <div>
      <Homepage page = "/activities" title = "Activities" />
    </div>
  )
};

export default Activities;